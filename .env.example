  # genai-ai-api-onpre — 環境変数サンプル
  # 設定面の雛形のみ。OSS スタック分（DB / Keycloak / SeaweedFS 等）を追加
  # 秘密値（HMAC 鍵等）は docker secrets で注入する方針

  # HTTP サーバの待受ポート
  PORT=3000

  # ログレベル（pino: trace|debug|info|warn|error|fatal）
  LOG_LEVEL=info

  # 実行モード（production|development）
  NODE_ENV=production
